{% extends 'base.html.twig' %}

{% block title %}Hello EtudiantController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		{{form_start(form)}}


		<button type="submit">Inscription</button>
		{{form_end(form)}}


	</div>
{% endblock %}

{# 
{% extends 'base.html.twig' %}

{% block body %}

	<table class="table">
		<thead>
			<tr>
				<th scope="col">Nom Complet</th>
				<th scope="col">Adresse</th>
			</tr>
		</thead>
		<tbody>
			{% for etudiant in mba %}

				<tr>
					<td>
						{{etudiant.nomComplet }}

					</td>
					<td>
						{{etudiant.adresse }}

					</td>

					<td>
						<a href="" class="btn btn-info">Details</a>
						<a href="" class="btn btn-success">Modifier</a>
						<a href="" class="btn btn-danger">Suprimer</a>
					</td>

				</tr>
			{% endfor %}

		</tbody>
	</table>
	<div class="d-flex justify-content-center">
		{% do mba.setPageRange(2)%}
		{{knp_pagination_render(mba,'user/pagination.html.twig')}}
	</div>
{% endblock %}
 #}
